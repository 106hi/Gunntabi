<%= form_with model:[post, post_comment], local: false do |f| %>
  <div id="comments-error">
    <%= render 'layouts/error_messages', model: f.object %>
  </div>
  <%= f.text_area :comment, placeholder: "コメントをここに", size: "45x5", class: "comment-form" %><br>
  <%= f.submit "送信" %>
<% end %>